{
  "name": "maybe-extra",
  "scripts": {
    "format": "elm-format src",
    "test:format": "elm-format --validate src",
    "test": "elm-verify-examples && elm-test && npm run test:format"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.2",
    "elm-test": "^0.19.1",
    "elm-verify-examples": "^5.0.0"
  }
}
