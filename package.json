{
  "name": "maybe-extra",
  "scripts": {
    "format": "elm-format src",
    "test:format": "elm-format --validate src",
    "test": "npm run test:format"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.2"
  }
}
